# Backlog

Where the code does not yet match `docs/PATTERNS.md` or `docs/UI.md`. These are
findings, not licences: new work follows those two files. A row leaves when it is
fixed, and a backlog that keeps solved rows stops being read.

**Each of the two sections holds exactly one table, and that is enforced.**
`src/backlog.test.ts` reads a section's rows as the pipe lines running
unbroken from the header row, and reports any pipe line arriving after a
non-pipe line has closed it — because three rows once shipped separated from
their header by a blank line, rendering as prose while a `[checked]` rule
counted them. The cost is deliberate and worth naming: prose followed by a
second table under the same `## `, or a `###` sub-heading with a table of its
own, **fails the build** rather than being quietly half-read. That is the
loud direction, and the author most likely to meet it is one adding a worked
example — which belongs under a `## ` heading of its own, where the reader
does not look for rows.

**A row is a rollback pointer, not a narrative.** Four cells, none of them
empty: what the gap is, where it lives, what the fix is, and what it is waiting
for. The whole row stays under 600 characters and is written to 575, so the next
edit to a row is an edit rather than a build failure. **Every row names what it
is waiting for** — a row with no trigger is a row nobody can ever retire — and
that rule is now [checked] rather than held by habit: `src/backlog.test.ts`
fails on an empty cell, an over-long row, a struck-through row, or a table it
cannot find.

Reasoning that outgrows a row belongs where such things live: the feature doc
that raised it, `docs/PATTERNS.md` or `docs/UI.md` where it is a general rule,
`docs/SPEC.md` §13 where it is a settled design decision, or a comment at the
code it governs. The rows as they read before they were trimmed are in this
file's own history and in that of `docs/PATTERNS.md` §11 and `docs/UI.md` §12,
whose headings now point here.

Add a row when a review finds a gap it is not fixing in the same pass.

## Patterns

| Gap | Where | Fix | Waiting on |
| --- | --- | --- | --- |
| A config edit reaches promoted properties through the structural arm | `parse/promoted-fields.ts`, `components/table.ts` `configError` | A known Table whose `columns` will not configure returns `{}` from `scopeValues`, so it publishes nothing and its promoted rows are *retired* — strip and restore while an author edits. The spec revision closed the *formula* churn path; this is the *config* one. Not a defect: the spec licenses the shape ("the suffix went") and the editor reports the error where it is fixable. | a report of properties flickering during a config edit |
| A correctness property is held by the order of two `addEventListener` calls across a module boundary | `components/card-face.ts`, `interaction/editable.ts` | An `atRest?: string` on `EditableOptions`, so the binding owns the pill's display swap and the ordering cannot be stated. Not taken on §1: one consumer. The order is a comment at the swap and two Card set cases go red on it, so no violation ships — but `editable.ts`, which owns the baseline, cannot see the rule. | a second control wanting a display that differs from its editing baseline |
| A delta assertion is weaker than a write assertion, and nothing says so where the next author will read it | `src/components/table.test.ts`, `docs/PATTERNS.md` §10 | §10 gains the rule — a delta says what was sent, only the write says what the note now holds — with the cheap form named: carry the delta through the component's own `write` and assert the section text. Twice in one feature a case asserted the delta and missed the note; the second time it was a reported bug. | the §10 edit, which belongs in a docs commit |
| Nothing catches an assertion entailed by the filter that produced its input | `src/styles.test.ts`, `src/components/contract.test.ts`, every source scan | Nothing cheap: it stays a review concern. The heuristic — the assertion's literal appears in the filter above it — fires on every legitimate `filter(x).every(x-ish)` in the suite, and an allowlist for that is a backlog kept in a test file. §10's floor half is done and true everywhere. | a second entailed assertion; two are evidence the heuristic is worth its noise, one is evidence review works |
| Nothing checks that two selectors do not declare the same rule twice | `src/styles.test.ts`, `styles/sheet.css` | Normalise every rule body and diff them, in the scan that already parses them. Measured: identical declaration sets hit 42, 16, 7, **3**, 2, 0 at thresholds of one to six declarations, so ≥4 is the usable line. The three at ≥4 are a Card set label against a Table label, a table's head against its foot, and two fields' focus. | a decision on the three, taken as its own diff; the guard lands with it |
| Four extracted modules' cases live beside the editor rather than beside them | `editor/config-panel.ts`, `editor/schematic-gestures.ts`, `editor/tree.ts`, `editor/layout-file-row.ts` | Split by what a case needs. Some need the real pane — a delete ending on the next file — and a sibling cannot import `layout-editor.test.ts`'s own pane harness, nor may §2 hold cases in `src/test/`. Others could run on a stub host: the row's note sentences, option order and labels, nothing selected with no file open. | the pane harness promoted to `src/test/` as a designed fixture |
| §10 does not name a module cut from the editor that has an entry point and is tested through the pane on purpose | `docs/PATTERNS.md` §10, `src/editor/`, `component-picker.ts`, `component-preview.ts` | A scan listing every `src/editor/` module with no test file, so each is either named by one of §10's exceptions or reported. Today the entry-point-and-output bullet says such a module earns a file, and six are held by the pane instead, which nothing in §10 states. | the §10 edit and the scan, which belong in one docs-and-test pass |
| A refused edit's complaint outlives the text it was about | `editor/field-error.ts`, `editor/config-panel.ts` | Either a complaint belongs to the text and goes with it, or to the field, whose value is restored rather than reverted. Settled per branch: a formula field keeps the typed text, so its complaint is about it (`formula-field-errors.md`). What is left is the branches that revert — `number`, the label, the four positions, the column count — where a red field says "Whole number, 1 or more." above a 7. | a ruling on which half governs a field that reverts |
| No component has a boolean that redraws the editor, so the redraw's focus path is only half driven | `src/editor/layout-editor.test.ts`, `components/*.ts` | The test asserts the precondition — a boolean's checkbox carries the `data-sheetsmith-focus` token that addresses it — rather than pressing one and watching focus survive. `Collapsible` was the only such boolean and went with the group's collapse. Not fixed by inventing a boolean no component needs, nor by a fake component in the registry. | a component gaining a boolean that controls another field's visibility |
| The view's share of a reader's posture has no test, and there are two of them now | `view/sheet-view.ts` | Six lines with no logic: two `Map`s, the `clear()` on a file change, the get/set handed to a component's context. Forget the `clear()` and a reopened note inherits the previous note's tab *and* its open records. Both halves are tested in the components. Not extracted — two consumers of a `Map` earn duplication (§1). | a pass of its own; the blocker is gone — a real `SheetView` renders in `sheet-view.test.ts` now — so this is a diff rather than a decision |
| §10 covers a test that passes vacuously and not one whose green depends on which case ran first | `docs/PATTERNS.md` §10, `src/components/isolation.test.ts` | A clause: a cold cost charged to whichever case pays it first is a green that a rename or a `.only` takes away, so it belongs in a `beforeAll` or in `vitest.config.ts`'s `testTimeout`, never in one case's third argument. Measured: 2.5s idle, 6.3s loaded, against a 5s default. A scan for that argument is refused — one instance is evidence review works. | the §10 edit, which two rows already wait on |
| A fifth list field imports from one of the other three, and one import is not geometry | `editor/list-fields.ts`, `editor/modifier-definitions-field.ts`, `editor/nested-list-field.ts` | `editor/list-geometry.ts` takes `listField`, `labelled`, `addControls`, `addControlSpacers`, the `--sheetsmith-list-fields` count they are sized by, and `ListContext`, which the nested list takes for `confirm` alone. `setOptional` is not geometry but the file-format rule that an empty field means an absent key. | nothing but a pass that does only that, and it has to move all three |
| One sentence about an empty `changes` list is stated in four places, held together by nothing | `editor/modifier-definitions-field.ts` (three sites) | The parser's reading is now `changesOf(raw)` in `parse/modifier-definitions.ts`, shared with the paste report, the fifth site. The editor's three still spell it: a count, an amount check and a drawn-but-detached entry, each with its own observable, so each has to be read through `changesOf`. Drift is quiet: the editor draws a line the report says nothing about. | a pass moving the editor's three onto `changesOf` |
| An extracted line rule is a module rather than a check, so a fourth field can spell it inline | `editor/field-lines.ts`, `editor/*-field.ts` | A scan of `src/editor/` for a `.split('\n')` followed by a `.trim` on the mapped line, outside `field-lines.ts` — `pointer-gestures.test.ts`'s shape. Not added when found: two of the three original copies merged in that same pass, so the scan would match nothing, which is the vacuous pass §10 forbids. | a fourth field of this kind, or a second inline spelling anywhere in `src/editor/` |
| One editor field module still has no test file, under a rule that now says it should | `editor/function-library-field.ts` | `function-library-field.test.ts`. §10 now carries the rule this row settled — a module in `editor/` with its own entry point *and* its own reportable output earns a test file, while the five true fixtures have neither — and `modifier-definitions-field.ts` was brought into line. The function library has both and no file. | the file, which is a diff of its own and no longer a decision |
| A declared property name is still a free string in one member | `types.ts` `ConfigFieldSpec.visibleWhen`, `card-set.ts`, `pool.ts` | A fourth `ConfigFieldSpec` parameter for the sibling key, filled by `ConfigFieldOf` with the whole of `DeclarableKey<TConfig>` — covariant like the other two, and unreachable from `TKey`, which knows only its own key. Measured: `visibleWhen: { key: 'sizzing' }` compiles, `conditionMet` finds no field and returns false, so the field silently never renders. | a pass widening the spec's parameters, which `entryColumns` fenced off |
| The reorder and remove controls of a list field are spelled twice in one file | `editor/list-fields.ts` | `addControls` names the item it acts on — "Move Strength up" — and asks before removing an entry carrying a hand-written formula; `renderEntriesEditor`'s pair says "Move up" and asks nothing. Sharing them changes three accessible names and adds a confirmation. §1 allows two copies only under a test driving both over the same cases, and these differ. | a decision on the accessible names: collapse into `addControls`, or a guard test — the rule is now `docs/UI.md` §6's naming rule |
| Nothing catches a fourth hand-rolled copy of the create-element helper | `src/ui/element.ts`, `components/`, `src/class-tokens.test.ts` | A scan of `components/` for the triple: `createElement`, a class set on the result, `appendChild` of it. `class-tokens.test.ts` is not that guard: it reads a class name that is a literal, every copy passed a variable, and against the pre-extraction tree it reports nothing. Narrow, the triple proves little; wide, it fails the build on every painter. | a fourth copy appearing, or a ruling that the isolation eslint rule can carry it |
| A scan asserting its own narrowness is tested in one direction | `docs/PATTERNS.md` §10, `src/styles.test.ts`, `components/isolation.test.ts`, `src/class-tokens.test.ts` | §10 gains the other half: a scan claiming narrowness owes a case asserting the spellings it must *not* report. Plus one reader shared by the scans, in `src/test/` (§2) — skip comments and strings whole, count parentheses, read an argument only when it is one literal. A false positive fails the build; a vacuous pass only waits. | the §10 edit, a reading of the older scans, and the shared reader |
| A mirror of `renderSheet` is spelled five times, and one let a defect ship | `worked-examples.test.ts`, `vault-fixture.test.ts`, `reset-flow.test.ts`, `promote-flow.test.ts`, `starters/index.test.ts` | The ~25-line head — three parses, the `walkComponents` map, `buildSheet` — into `src/test/` (§2). §1 extracts at three, and it rewrites `sheet.test.ts`'s host scan. | a pass that does only this. **Not tidiness**: `reset-flow.test.ts` mirrors `applyTrigger`, so it cannot see a synchronous trigger's order against an async render, and the reset undo shipped broken on every promoting layout. |
| Nothing holds the double's comments about the app to the app, and it has let six defects through green | `src/test/obsidian-stub.ts`, `obsidian-stub.test.ts` | A probe pulling the app's behaviour from the installed asar, failing when the double disagrees, as `calibrate.mjs` does for CSS. Every one was a comment asserting what Obsidian does, wrongly, every test agreeing. The sixth: `TextFileView` models no `onload`/`onModify`/`lastSavedData`, so the platform's merge on a foreign write is unseen here | a decision on the cost: a probe reading the installed app cannot run in CI |
| `loadSettings`'s two guards are untested, and a correctness argument now rests on one | `src/main.ts`, `src/settings.ts`, `src/test/plugin.ts` | Either the guards move to `settings.ts` as an exported pure function — drivable in three lines, and a better fit for "keep `main.ts` to plugin lifecycle only", but an amendment to an approved spec, which pins them to `loadSettings` by name — or a `Plugin` a test can `onload`. `createCharacter` cites the trim as a precondition it does not repeat. | a ruling on whether the guards may move, or the plugin-shell fixture |
| `commands.ts`'s undo/redo `Notice` feedback is untested | `src/commands.ts`, `src/test/obsidian-stub.ts`, `src/test/plugin.ts` | Nothing asserts that "Undone."/"Redone." fires only when the view actually undid something, and no command in this file has shell-level coverage. `Notice.messages` makes the assertion cheap; reaching a `checkCallback` needs `Workspace.getActiveViewOfType` and `Plugin.addCommand` on `fakePlugin`, which `plugin.ts`'s header reserves. | a real need for either member, or a decision to build both just for this |
| The double writes into a folder that does not exist, where the app refuses | `src/test/obsidian-stub.ts` `Vault.create`, `editor/layout-import.test.ts` | Model `ENOENT`: refuse a `create` whose parent is in neither map. `layout-import-export.md`'s failure table names a name carrying `/` as a write the vault refuses, and the double writes it happily — so that arm is asserted through the one refusal the double does honour, a file where the folder must go. | a reading of what the change breaks across 81 test files, which is wider than the four lines it takes |
| A vault-wide file count cannot be taken, so a refusal is proved over one folder | `src/test/obsidian-stub.ts`, `src/editor/layout-import.test.ts` | `Vault.getFiles()` on the double, which `layout-import-export.md` reserves for the deferred file-chosen import. Issue 516's criterion is that a refused import leaves the vault as it was, counted rather than probed; the count runs through `listLayouts` over the layout folder, which is everywhere this feature can write and not everywhere a future one could. | the file-chosen import, which needs the same member |
| An inline field error is neither announced nor linked to the input it is about | `editor/field-error.ts`, every caller | Either `showFieldError` owns `aria-invalid` and an `aria-describedby` to the div it draws, a signature and policy change reaching every field in the pane, or each caller sets them. `function-library-field.ts` sets both plus `role="status"`, so one surface announces and thirty say nothing — two of them now **on open**, before the reader acts: a broken reset expression, a repeated modifier name. | a ruling on whether the shared helper owns the attributes |
| The suggestion popup carries no ARIA, and its input can only say a list may appear | `editor/formula-suggest.ts` | `aria-expanded` and `aria-controls` on the input, pointed at the popup. Unavailable: `AbstractInputSuggest` declares neither `isOpen` nor `suggestEl`, so both would read members a release can remove silently. `aria-autocomplete="list"` is the honest half, and the panel's inventory holds every name the popup offers, keyboard-operable | the platform exposing the popup element, or a surface this plugin owns |
| The suggester restates a rule two components own, and nothing holds the copies together | `formula/vocabulary.ts` `rowNamesOf`, `components/table.ts` `rowScope`, `record-set.ts` `recordValues` | A registry-wide case: the per-row names published for a `columns` field equal the names that component's own stored row scope holds. Unwritable today — an inner scope is a resolve-time fact no declaration states — so it needs a contract member. `'computed'` is spelled here, since nothing in `formula/` imports `components/` | that contract member, a design decision rather than a test |
| The one bare global event constructor in `src/` builds the wrong realm's event in a popout | `editor/formula-suggest.ts`, `docs/PATTERNS.md` §5 | Nothing available. `input.win` is typed `Window`, which declares no constructors — §5's own `prefer-create-el` gap; casting past it trips `obsidianmd/no-global-this` and `--max-warnings 0` fails the build; `activeWindow` types the same and is not in the stub. No failure today: nothing does an `instanceof Event` | the typings gaining the realm globals, or a ruling that the lint rule may be disabled at one line |
| The sheet leans on the base class's vault event without owning one | `view/sheet-view.ts` | `TextFileView.onload` registers `vault.on("modify")` and merges an external write into a dirty view; `SheetView` overrides no `onload`, so it inherits that and registers none. `reload` therefore has to decline a dirty view, and the ordering in `persist` stands in for an event the tests cannot drive. Registering our own would make the sequence explicit and testable. | a ruling on duplicating a platform behaviour in order to test it, which is row 61's question |
| A module named for modifiers holds four exports with no modifier in them, and the walk it calls single is spelled in four places | `formula/modifier-targets.ts`, `formula/resolve.ts` `publishedFieldNames` | Rename it to the job its header claims, "the single walk over `ScopeValues`", and fold `publishedFieldNames` into `publishedEntries`. That needs `formulaTexts` moved to its one consumer first: that import is the cycle keeping the walk out. Five modules import the file. | one pass doing the rename, the move and the merge, plus a destination — `published-names.ts` is taken |
| The blank-name restore is spelled twice, and the binding's reason for not owning it has expired | `interaction/editable.ts`, `record-set.ts`, `track.ts` | A `restoreOnBlank` option on `EditableOptions`, putting the departure in the module that owns the rule. Refused when a record's name was its only consumer; a Track row the character named is the second. The sentence is shared now (`keptRatherThanBlank`), the mechanism is not: a blank is not a `refuse`, and the stored value returns through `handle.sync`. | a third consumer, or a ruling that two earns an option here |
| The panel form field is built twice, and the two builders agree on the classes and nothing else | `components/modifier-form.ts`, `components/track.ts` | A `panelField` on `ui/anchored-panel.ts`, with both moved onto it in one pass. Not taken at two: the classes are already single-sourced in `shared.css`, and what differs is behaviour — one returns the row and takes a `wide` flag, the other returns the input and needs none. Sharing now buys a knob per caller. | a third panel form, which would settle the signature by use rather than by guess |
| An empty-state line three components draw is named for one of them | `styles/sheet.css`, `components/table.ts`, `roster.ts`, `track.ts` | A shared `.sheetsmith-empty`, all three moved onto it, a `docs/UI.md` §9 row, and Track's own size-and-padding rule (`.sheetsmith-track > .sheetsmith-table-empty`) folded into it. The rename picks one behaviour: at the 2×1 insert size Table and Roster clip the line on one row, while Track wraps it and grows the row. | a pass that does only the rename, since the owner kept it out of the bare Track fix |
| §3 puts constants before `XConfig`, and four components declare most of theirs after `XData` | `docs/PATTERNS.md` §3 item 3, `components/track.ts`, `pool.ts`, `table.ts`, `roster.ts` | Either §3 says a constant sits beside the helpers it serves, after `XData`, which is where these four keep most of theirs, or the four move their constants up above `XConfig`. Open which: the doc reads as a rule, the code as the practice. Found reviewing the bare Track fix, which added two constants the same way. | a ruling on whether the doc or the code changes |
| The depth rule is stated twice, for a file and for a move, held together by a sweep | `parse/layout.ts` `parseChildren`, `editor/reparent.ts` `tooDeepHolders` | The depth fact is already one function, `mayHoldChildren`. The rest differs on purpose: the parser refuses any `children` key two deep, the move only a non-empty one, since `reparent` drops the empty ones. One predicate would change one rule. The guard is `reparent.test.ts` "every move canReparent allows saves", red on the defect this came from. | a ruling on whether the parser accepts an empty `children` list at any depth |

## UI

| Gap | Where | Fix | Waiting on |
| --- | --- | --- | --- |
| A Tab set's tree order can differ from its tab strip | `parse/layout-walk.ts` `walkComponents`, `editor/reparent.ts`, `components/tab-set.ts` | The strip reads the file's order; the tree sorts by stored row. A tab moved in through the tree gets the set's next free row, so from then on Move up changes the strip and not the tree, and stays enabled. Either the walk keeps file order where children are not placed, or a move into a Tab set gives the tab row 1. `layout-editor.test.ts` holds it as an `it.fails` case. | a pass allowed to touch the walk's sort or `reparent.ts` |
| Dragging a grip handle drags only its icon | `editor/tree.ts` `bindDragSource`, `editor/list-fields.ts` both handles | No handle calls `setDragImage`, and each handle is a button dragging itself, so the ghost is the grip glyph while the row stays put, on every tree row and list entry. Set the row as the drag image at `dragstart`, offset by where the pointer sits in it, so the whole card follows the pointer. | a pass on the drag affordance, taken alone |
| The canvas's arrow keys may be unreachable by pointer | `editor/schematic-gestures.ts` `beginDrag` | Reported, not confirmed: pressing a card on the canvas seems to leave it unfocused, so the arrow-key nudge never fires and the keys scroll the pane. Suspected cause: `beginDrag` calls `preventDefault` on `pointerdown`, which also stops the press focusing the overlay. If so, focus the overlay in `beginDrag`. The tree's placed-grid refusal points readers to these keys. | a repro in the harness or the app |
| A Track row's fields are 12px tall against a 20pt pointer minimum | `.sheetsmith-track-row-name-input`, `-length-input` | The row's pitch, for every Track at once. Measured 79.6x12 in a 21px row, against `legibility.md` §5's 20x20. **The 9px gap does not discharge it**: that clause is about a target overflowing its box, as the ring's does and these do not. Padding one field makes a mixed card ragged, 21px against 29px; padding the row re-pitches every spell-slot card. | a ruling on the Track row's pitch, a decision about every Track rather than this feature |
| The Add panel lands focus in two places on two cards of the same kind | `components/track.ts` addForm, `ui/anchored-panel.ts` `focusFirstControl` | Either focus **Name** whenever the form is drawn, or keep list-first and record it. `focusFirstControl` takes the first focusable, so a card with a declared row still on offer lands on that line and one with none lands in the field — press **Add** and type, and only one takes the text. Both are defensible, and list-first predates this feature. | a decision on whether the list or the form is the panel's primary offer |
| A Track row name is 2.13:1, and one is now the character's own typed text | `.sheetsmith-track-row-name`, `-name-input` | One ruling for the name column. Sampled `#ababab` on `#f6f6f6` and `#666666` on `#282828` at 10.2px against `legibility.md` §3's 4.5:1. Pre-existing `--text-faint` on **every** Track row name; what is new is that one is editable, so it is the string a reader reads back to check what they typed. Lifting that half alone breaks the one treatment the design rests on. | a ruling on the name column's colour, which moves every Track on every sheet |
| Promoting a computed column's `.value` is a silent dead end | `parse/promoted-fields.ts` `promotableNames` | Either withhold `.value` where an entry stores nothing, or say so at the picker. `buildSheetScope` registers the thunk and it answers `undefined` for ever, because a computed column stores nothing — so such a row sits for ever in the transient arm: no write, no report, no explanation. Predates this feature and matches what the suggester offers. | somebody promoting one and asking why nothing happened, or a pass over what the suggester offers |
| A marked list row drops its other controls, and the one fixed list shifts its trash 3px | `styles/editor.css`, `editor/list-fields.ts` | `align-items: start` on a marked row fixes the 16px drop for the promoted-fields list, via a class its field recomputes from the DOM; the other three share the geometry and still drop. Residue: the trash pitch reads 46/46/**43**, the icon being 24px against a 30px control. Both alternatives are worse — `align-self: center` restores the recentring, 3px of margin is a magic number. | one look at a marked row across all four lists, which no shot stages |
| A modified number's mark is a plain underline, so it reads as a link | `styles/sheet.css` `.sheetsmith-modified` | Nothing the stylesheet may do while the review stands: dotted, `--text-muted` and 2px are `text-decoration-style`, `-color` and `-thickness`, which `obsidianmd`'s CSS lint calls partly supported, and a bare line value is all it takes. `border-bottom` keeps all three and was reverted: it draws on the box, so a right-aligned number took a rule across its cell. 34 of 80 shots moved | a ruling on whether a clean report is worth the mark |
| Three hand-rolled `<select>`s in the editor reserve 2.4em for a chevron nothing draws | `editor/list-fields.ts` — **Holds**, **Control**, and the sense select | `cls: 'dropdown'` on each. Bare, they inherit `select`'s `--dropdown-padding-end: 2.4em` (31.2px of 112px) while the chevron lives on `.dropdown`, so a third of the box is reserved for a mark that is not drawn and a select is pixel-identical to a text input. Applied and reverted: pre-existing controls in a pane about to be reviewed. | a pass that does only this: three lines and a look at the shot |
| A breakdown lists contributors that did not move the number it is about | `formula/sheet.ts` `sheetModifiers.breakdown`, `formula/modifier-targets.ts` | Gate the mark on `acceptingTargets`' own `relative` local — "this component's formula mentions `mod.self`" — already computed: one member on `SheetModifiers` and four lines. That set ORs two rules, only the first can move a value, so a card whose formula reads no slot still draws the mark. The picker keeps the wider set: it is about what an author could wire. | a decision that the mark follows the narrow set |
| The test DOM accepts markup a browser refuses, so an aborted render is invisible to the suite | `src/test/obsidian-stub.ts`, happy-dom | The instance is closed twice over, by `ui/element.ts` and `class-tokens.test.ts`'s scan, but the class is open: any DOM call that throws only under a spec-compliant implementation is a render this suite signs off on. The stub cannot reimplement `DOMTokenList` validation, and "the render drew every row" is a check per component. | a second such call: an `insertAdjacentHTML`, an invalid `setAttribute` name, a rejected selector |
| Focus cannot return to content the app has not finished rendering | `view/cell-focus.ts`, `components/rich-text.ts` | The restore waits until every render the pass started has landed, changing the view's render loop and costing a focus jump on every rebuild. No identity scheme helps: the anchor does not exist yet, so `data-href` finds nothing an index did not. The loss is fixed — a past-the-end index lands on the cell's last control — and the miss is the anchor the reader wanted. | a second component that draws asynchronously, or evidence that the miss is felt |
| An editor field clips its value, where a message may name a clipped character | `editor/list-fields.ts`, `editor/config-panel.ts`, `ui/truncation.ts` | `revealWhenTruncated` on the pane's text fields — a roster decision for all of them rather than two: its four consumers are all on the sheet and no editor field has called it. Measured: 226px of cell against 38 stored characters. Neither instrument can check it, since happy-dom reports `scrollWidth` 0 and a `title` never appears in a shot. | a ruling on whether the pane's fields reveal, taken once for every field |
| A quoted app path can end a line on its arrow, and one untried binding removes it | `src/settings.ts` — the **Character folder** description | Bind each arrow to the label *after* it, not before: longest run `→ Default location for new notes.` is 187px in the 286px column, zero overflow at 620 and at Full, no line ending on an arrow. Cost: a line then *begins* with an arrow, reading as a continuation rather than a step. Binding the whole path overflows by 48px, re-measured — 36.9px was the pre-group column | a decision on whether a leading arrow reads worse than a trailing one |
| The character folder's shipped state carries its only destination hint at 2.3:1 | `src/settings.ts` — the **Character folder** placeholder | Nothing the plugin may do: `--text-faint` on `--background-modifier-form-field` through the app's own `setPlaceholder`, and `UI.md` §1 forbids substituting a colour. Sampled: `#ababab` on `#ffffff` is 2.30:1, `#666666` on `#2e2e2e` 2.36:1, against 4.5:1. Empty is this row's shipped default, so the placeholder is what nearly every reader sees. Mitigated: the description repeats it verbatim | a decision to stop carrying this hint in a placeholder |
| The stat note clips mid-word, and Image's reference field is worse | `.sheetsmith-card-note-input`, `components/image.ts` | `text-overflow: ellipsis` and the full value in `title` through `ui/truncation.ts`, whose `revealWhenTruncated` now reads a control's `value` — the blocker is closed. Measured: at a 620px container "chain mail, shield" renders as "chain mail, sl" with room to spare, where the card label above shows "ARMOUR C…". Image's field is transparent unfocused, so a vault path cannot be read at all. | nothing but a pass that does it, for both instances |
| A totalled column's key field never checks it is a name, typed or stored | `editor/list-fields.ts`, `components/table.ts` | An `isName`-when-`total` rule on the field. It has none: the check lives only in `table.ts`'s `baseConfigError`, the whole-config rule the card runs, so a totalled column keyed "item total" draws a normal-looking Key field beside a broken card. Render-time validation had nothing to reuse, since writing a rule is not running an existing one. | a decision to add the rule to the field |
| No `<select>` in the editor carries an inline error across a rebuild, and a checkbox guard carries no token | `editor/layout-editor.ts` `restoreFieldErrors`, `editor/reset-field.ts` | Two gaps. The replay reads `HTMLInputElement` only and drops every other token, so an errors map on a select is a no-op by construction, pane-wide; **Acts on** survives only by being recomputed. The buffer guard's token sits on `toggleEl`, not the checkbox it anchors. | the select half, widening the replay; the checkbox half, § Patterns' ruling on a field that reverts, which reaches no select |
| The pane can hold a layout it cannot save, with only a transient notice to say so | `editor/layout-editor.ts` `persist` | None yet. `persist` serialises, re-parses, shows a `Notice` and returns — designed, per its own comment — but every later edit then fails to save too and nothing on the pane says the file is behind. Choosing **Set to a formula** on a binding with no expression is one gesture into it; that field now marks itself, which is the field's half and not the pane's. | a ruling on what a pane owes while it holds a layout it cannot write |
| An inline field error has no second channel in forced colors, and the sibling's border does not transfer | `styles/editor.css` `.sheetsmith-field-error` | `.sheetsmith-field-problem`'s leading border does not copy: this class is mostly right-aligned in `.setting-item-control`, `flex: 1 1 auto`, so its box spans the column while the sentence sits at the far edge — 151px border to text at 1500, 335px at 1000, where it lands under the description. A trailing border fits but splits the class. | a ruling on a right-aligned message's second channel, taken once for the pane |
| The pane's secondary labels outgrow its primary ones at a raised Text setting | `styles/editor.css`'s seven `--font-smallest` declarations | One call for all seven: follow the setting, or hold at `--font-ui-small` as a reset binding's two messages now do. At Text 24 they grow 1.5× while px-fixed tokens hold — 19.2px of muted secondary against 13px of primary (`legibility.md` §4). The seventh is `.sheetsmith-nested-label`, which then draws a list's name at the rank of the field labels inside it. | one ruling for every token in the pane, which is a pass of its own |
| A flag outlives the control that sets it | `editor/list-fields.ts`, `editor/modifier-definitions-field.ts` | Either clear a flag the new type cannot carry, or keep offering the control while the flag is set. **Show a total** and **Publish per row** are offered only on column types that can carry them: tick either on a number column, switch it to text, and the card reports an error naming a control no longer on screen. `applies` is deleted by the operator's own handler; `bonusType` is left, older than the phase. | one decision, taken once for all three instances |
| No harness view stages any of the field messages on a column's detail line | `harness/samples.ts`, `editor/list-fields.ts` | A state storing what each rule refuses: **Level names** under two entries *or* one with nothing before its colon, a **Levels** out of range, a **Minimum** that is not a number. None is reachable from a sample, so `field-render-validation.md` shipped them unphotographed. Staging one costs 45px into 28px of cap headroom (`SPEC` §13). | a fixture staging them under a cap that fits them |
| No view renders `prefers-contrast: more`, for any component | `harness/shot.mjs`, `styles/sheet.css` | Several `prefers-contrast: more` blocks — the card's border, the abbreviation, the unresolved glyph, a dropdown's chevron — have never been looked at, so a mark legible only because of one looks fine in every shot. Chrome offers no flag: `--force-prefers-contrast` renders a byte-identical PNG, and the feature needs the DevTools protocol's `Emulation.setEmulatedMedia`. | a reason to drive a browser at all, which is the fixture a hover or a press would need |
| Headless Chrome will not render a viewport under 500px, so nothing below that has been photographed | `harness/shot.mjs`, `docs/UI.md` §4 | A driven browser, which can set a device metrics override and has no floor. Measured: Chrome floors `innerWidth` at 500 whatever `--window-size` says, so every sub-500 shot is a 500px render cropped to the frame, which looks exactly like a narrow sheet that overflows. `sheet-narrow`'s 520 is the narrowest true viewport; its `&width=380` is a container width. | a reason to drive a browser, as the row above is |
| Obsidian's dropdown chevron is invisible in forced colors, so an editor `<select>` has no affordance | `styles/editor.css`, `app.css`'s `.dropdown` | A wrapper per select, so the plugin's chevron rides on a pseudo-element: markup rather than CSS, overriding the app's mark everywhere. Three cheaper answers fail, measured: Chrome forces a gradient `background-image` to `none`, keeps a `url()` one; a second data URI fixes one palette, breaks the other; a `<select>` has no pseudo-element. | one call for all six controls: the pane's own chevron everywhere, or the app's |
| An error card renders without its component name | `components/card.ts`, `view/sheet-view.ts` | A contract member the view asks *before* rendering, since the view can only prefix what it is handed and a render-time guard draws into the cell itself. The view prefixes a failed `read`, so one misconfiguration is labelled or not by whether the note has a body for it — and a `render` failure replaces the heading, so on a sheet of three dropdowns nothing says which to open. | that member. The row stands for every component whose read failure comes from a hand-edited note |
| A band of columns balanced to end together holds only if each ends in a stretcher | `docs/UI.md` §4, `src/starters/*.json`, `components/tab-set.ts` | §4's "a component fills its grid placement" is true of §9's boxes-sized-by-placement roster, false of tables and containers, whose placement is only a floor. A tab set is a sound foot only with a stretcher at the foot of its tabs, nor even then: a panel is as tall as its tallest sibling tab, a measured 404px void under three. Figures: `starter-layouts.md`. | a ruling on splitting §4's sentence into the two families |
| A wide table with few columns clips its first and last while a middle one takes the slack | `components/table.ts`, `.sheetsmith-table` | A column-width rule for a table with more room than columns: whether the name, the last column, or all evenly absorb the slack. Measured on `magic_items` at 1400px: `Item` gets 102px for a longest cell of 130 — every row's only identifier cut — while `Notes` holds 626px and ends 440px short. Widening `Item` only moves the slack. | a decision on which column absorbs slack, which two modifier tables side by side make worth taking |
| `hideLabel` puts a component out of line with labelled siblings | `components/group.ts`, `image.ts`, `rich-text.ts` | Three fixes, one blocker: always reserve the slot (a gap over a lone component), never (today), or let something knowing the row decide — a config key or a `RenderContext` member, since a component cannot see if it has a labelled sibling. Measured: in a row of six frames the unlabelled box top sits at +0px against +19px. Worse stacked, where the heading is the only mark. | that decision; `card-face.ts`'s `reserveAbbreviation` answers it for cards |
| No fenced component's read error is drawn except one | `harness/samples.ts` | `brokenSamples()` breaks *config* — a key, a column, a row, a child — so no section holding something its component cannot parse is rendered for Card, Card set or Pool, which is the state a hand-edited note arrives in. Track's flag now carries a broken body and its error is in `sheet-error.png`; the next one needs only an id and a body. | a choice per component of which breakage is worth a picture: a malformed fence, a duplicate key, a value of the wrong kind |
| A disabled control looks exactly like an enabled one | `editor/layout-editor.ts`, `styles/editor.css` | Four lines of `editor.css`, plus a call on the three other reorder controls, which never disable their ends — styling this one alone makes it the odd one out. The tab-order arrows are the first control to call `setDisabled`, and Obsidian carries no `is-disabled` rule for `.clickable-icon`, so all six render identically. Behaviour is right and tested; state that never reaches the paint is half a control (§6). | a decision covering the remaining reorder controls (the tree's are now a menu) |
| One of the editor's five add controls is a call to action, against the pane's own rule | `editor/list-fields.ts`, `editor/layout-editor.ts` | Either drop `mod-cta` from **Add row** or argue it at the call, which carries no comment. The pane decided this once the other way — **New layout** is `mod-cta`-free, fixed by a test reading "creating a layout is not this pane's primary action" — while **Add reset**, **Add row value**, **Add modifier** and **Add promoted field** all sit tertiary beside an accent **Add row**. | a decision on which editor action, if any, is primary |
| The harness bar splits a group across two rows | `harness/index.html`, `harness/theme.css` | A group element per label-and-buttons, with `white-space: nowrap`. Nothing ties a label to the buttons it names, so wherever the bar wraps a group can break, and in the middle: measured at 1000px, the `settings-light` frame, **Both** drops to row two alone ahead of **State**. The bar is `flex-wrap`, so its height comes off every shot at every width where it rewraps. | nothing but a pass that re-measures the default widths |
| A bad drag is marked by border colour alone, so forced colors shows none of it | `styles/editor.css` | `.sheetsmith-preview-overlap` and `-clamped` recolour a border, and forced colors repaints every border one system colour. The selected block's fix was a wider outline at a different offset; `clamped` cannot copy it, marking *one edge* to say which side stopped growing, and an outline has no one-edge form. Neither state can be looked at: both exist only during a drag. | the same fixture a hover or a press would need — take it with the `prefers-contrast` row |
| The layout editor pane has no narrow regime, and does not fit below about 470px | `styles/editor.css`, `editor/layout-editor.ts` | Two independent contributors, confirmed by hiding each other: the `.setting-item` rows do not stack, and stacking them as the app does on mobile removes that half; the schematic's twelve-column grid overflows on its own. Never seen: no editor shot went below 1190, and there is no width-gated narrow settings CSS to borrow — every such rule in 1.13.7 is `.is-phone .modal` | a decision on what a twelve-column schematic does at phone width |
| Three same-size circles in one summary line mean focus, on, and off | `styles/sheet.css`, `components/record-set.ts` | Not wrong, hence a row rather than a fix: focus and state do separate on fill against outline, which `legibility.md` §2 accepts as a second channel, and one treatment applied consistently is what §6 asks for. What earns the row is the control: the delete is the only irreversible thing here, and a mark read twice is worse there. | a decision on whether a destructive control's focus differs in shape from a state mark's — one call for three glyphs |
| A six-field record's fields wrap inside their own grid track, so line two starts at the fields' column | `styles/sheet.css`'s 320px container query, `components/record-set.ts` | A threshold keyed on the field count, not a fixed width. 320px was derived for "a name, a labelled number, a ring and a glyph", while a record's fields are the layout's; above it they sit in an `auto` track that shrinks and wraps within itself. Measured at containers of 338, 346 (`sheet-narrow`, a default shot) and 361px. | a threshold call of its own, with the shot at those three widths |
| A stacked cell renders both its layers at once in forced colors | `components/table.ts`, `.sheetsmith-table-linked` | Record set's departure applied to Table: hide the layer under `@media (forced-colors: active)` rather than leave the field transparent, giving up the cell's link press there. `color: transparent` is not transparent there: every foreground is repainted, so the field's text returns under the opaque layer and a linked cell is unreadable. | a pass on Table that takes the same departure, or a ruling that a cell keeps its press and loses something else |
| Entering edit mode on a Rich text block loses the reader's place | `components/rich-text.ts`, `.sheetsmith-rich-text-box` | A proportional map of the offset on focus, a few lines — honest only if the caret moves too, reversing departure 2 in `rich-text.ts`, where a rendered point is deliberately not a source character. Declined when offered. The alarming half is fixed: the field opened at its last line; `setSelectionRange(0, 0)` makes the landing chosen. | evidence that the jump is felt on a backstory long enough to scroll, the one length the sample lacks |
| `mod.self` reaches a player on a computed cell's popover | `components/table.ts`, the computed cell's `title` | A glossary line on the popover, or a plain-English gloss beside the formula; both add a surface. The formula line stays, and the reason is a comment at the code: a breakdown line is a sentence this plugin composes and owes the reader's vocabulary, while a formula line is a quotation of the author's own text. The residue is that `mod.self` names nothing a player can see. | an author asking for one |
| A list entry labels its first line once in a header and its second line on every entry | `styles/editor.css` `.sheetsmith-field-name`, `editor/list-fields.ts` | Drop the header row and label per entry on all four lists, a pane-wide change to the labelling model. The detail labels cannot move into a header: the line is `flex-wrap: wrap`, so a header would not align at every width. Shared geometry, not a Modifiers defect: that list shows `Name` once over ten entries and five other labels ten times each. | a decision on the labelling model, for all four lists at once |
| `--text-error` is 4.20:1 in the light theme, and 4.03:1 on a settings row | every `.sheetsmith-error`, `.sheetsmith-field-problem`, `.sheetsmith-field-error` — including the tree's refused chords and pastes, and the paste box | A companion channel, not a different red, as forced colors got a border. `#e93147` is **4.20:1** on `#ffffff` and **4.03:1** on a panel row's `#fafafa`, against `legibility.md` §3's 4.5:1; dark clears at 4.55:1 on `#232323`. A host pair the plugin does not own. | nothing. Recorded so each surface is a number, not a suspicion |
| The anchored panel is the largest surface on a sheet, and on a narrow pane it scrolls inside a table that scrolls | `ui/anchored-panel.ts`, `.sheetsmith-panel` | Nothing until the size can be seen. Six labelled fields, a list and a promote row, capped at `min(500px, 100vh - a gutter)` — a measurement now, replacing a `34em` that was arithmetic on the wrong unit — so the ordinary case fits at 479px with the app's `--input-height` on the controls, and a row with three parts and one open does not. | a viewport under 500px, which is the headless-Chrome floor row above |
| The panel does not follow the vault's text size, and nothing says whether that is right | `.sheetsmith-panel`, `harness/shot.mjs` `sheet-modifier-form-large-text` | A ruling, not a change. It hangs off `document.body` at `font-size: var(--font-ui-small)`, so neither the sheet's inherited size nor the reader's **Text** setting reaches it: measured at 390 x 479 both at the default and at `text=24`, beside a sheet that grew 650px. That is Obsidian's own regime, and §1 is why the plugin invents no type scale. Photographed, so it can be ruled on by looking. | a ruling |
| The harness draws neither the trigger bar nor the sheet's notice, so two surfaces have never been looked at | `harness/harness.ts`, `src/view/sheet-view.ts`, `missing-layout.ts`, `src/layout-picker.ts` | Three jobs: `renderSheet` must build the bar and wire `applyTrigger`, so the harness needs a note to write into; `ConfirmModal` and `SuggestModal` need an `App` the sheet surface lacks; a live **Undo** `Notice` that no still can capture. The trigger button, the rest confirmation and three notices are in no PNG. | a pass that gives the harness sheet a note to write |
| A derived starter shows "?" on the screen a user sees first | `src/starters/*.json`, `components/track.ts`, `formula/expression.ts` | Two engine changes ruled out of the starters run: publish `.left` as the full count where nothing is stored, and a `default(name, fallback)` primitive with a SPEC §5 entry. Cold "?" is structural: an unpublished name fails rather than defaulting to zero, and a layout cannot prefill values. Counts in `starter-layouts.md`. | SPEC §13's question on whether a derived sheet owes better than "?", and a decision on `track.ts`'s `.left` rule |
| A Rich text sample draws as plain paragraphs, not markdown | `editor/canvas.ts`, `components/rich-text.ts` `paintParagraphs` | The canvas passes no `renderMarkdown`, so a Rich text block takes its no-app fallback: wikilinks are live and every other mark draws as its own source. The sample deliberately holds no markdown, so a reviewer's view is honest — but an author's layout is judged against a canvas that draws *their* prose differently from the sheet. | a decision on whether an editing surface should render a character's prose; the pane has an `App`, so it can |
| The suggestion popup's hover and its flip at the foot of a window are in no shot | `harness/shot.mjs`, `harness/editor-pane.ts` | A driven browser. Obsidian places this popup from JavaScript, which `obsidian-stub.ts` models none of, so the four `editor-suggest-*` views place it from the field's own box — a flip and a height clamp cannot happen there, and `:hover` needs a real pointer | the same fixture the `prefers-contrast` and bad-drag rows wait on |
| The published-name inventory puts one tab stop per chip ahead of the form's first field | `editor/published-names.ts`, `editor/copyable-name.ts` | A roving `tabindex`: the block one stop, arrow keys inside it. Costs a keyboard model of its own in a pane whose focus restoration is token-based and runs on every redraw, so the block needs its own restoration story. Measured: 18 stops on a six-entry Card set, 56 on an eighteen-row table. Marked and ordered now, which was the sharper half | a real case to check it against — the roster fixture's ten chips are one, not yet driven through |
| A Pool's ceiling moves with no mark and no door | `components/pool.ts`, `.sheetsmith-pool-max` | Track's affordance, ported: a glyph `<button>` beside the card's name opening `modifierBreakdown` in the shared popover, on `.sheetsmith-track-modifier-button`'s treatment. The pattern exists now, so this is a port rather than a design — what is still Pool's own is the *mark*, since a numeral after a slash is neither Track's segments nor Card's underline. `hp.max` is published and a modifier reaches the numeral, the bar and the clamp. | a Pool buffer-and-ceiling pass, which owns the mark |
| A granted tail and its own tooltip state one push at two magnitudes | `components/track.ts` `lengthPushed`, `components/modifier-breakdown.ts` | A sentence for a transforming formula — what was pushed, then what the formula made of it — a builder change reaching Card, Card set, Table and Roster. Measured: `count: 'floor((3 + mod.self) / 2)'` with `+2` draws one granted segment while the title reads `Total +2`. Half is pre-existing on a Card; new is a second *visible* statement on one control. | a ruling on what a breakdown says where the formula transforms the slot |
| In forced colors a Track says nothing at all about its value | `styles/sheet.css` `.sheetsmith-track-segment-fill` | Not one block: repainting the fill `SelectedItem` needs the granted ring measured against it, and `CanvasText` on `SelectedItem` is a contrast no palette guarantees. Measured, a run at 5 of 6 against one at 2 of 6: 62 of 2760 pixels differ, all antialiasing — the same picture. This feature's "both channels survive forced colors" holds of the *mark* while the value it qualifies does not survive at all. | a forced-colors pass over Track, against a real palette |
| A dead declaration sits in the segment rule, spelled like a property | `styles/sheet.css` `.sheetsmith-track-segment` | One word: `box-sizing: border-box`. `segment-sizing: border-segment` is no property at all and looks like a find-and-replace of "box" that caught a rule it should not have. Harmless — the measured widths are border-box because the host's own reset does that work — which is why it is a row rather than a fix, and why nothing reports it: a stylesheet drops what it cannot parse. Predates this feature. | any pass that touches this rule, which should take the word with it |
| The **Layout file** dropdown repeats the file explorer for every layout but an unconverted `.json` | `editor/layout-file-row.ts` | Decide whether the pane keeps a switcher. Choosing a file there is `host.openLayoutFile`, which a click in the explorer already does now that layouts are registered; the one file only the dropdown reaches is a `.json`, unregistered this cycle. The row's buttons are not in question: they act on the folder or the open file, not on which is open. | `.json` reading ending, which leaves the dropdown no file the explorer cannot open |
